{
  "name": "typescript-patterns",
  "version": "1.0.0",
  "description": "A set of design patterns implementations in Typescript",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node build/$EXAMPLE/demo.js"
  },
  "author": "Gabriel Fusca",
  "license": "MIT",
  "dependencies": {
    "typescript": "2.4.1"
  }
}
